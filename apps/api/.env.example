# Turnkey (Wallet Infrastructure)
TURNKEY_API_PUBLIC_KEY="<get_from_turnkey_dashboard>"
TURNKEY_API_PRIVATE_KEY="<get_from_turnkey_dashboard>"
TURNKEY_ORGANIZATION_ID="<get_from_turnkey_dashboard>"

# Solana (Network)
SOLANA_RPC_URL="https://devnet.helius-rpc.com/?api-key=<your_helius_api_key>"

# Backend Authority Keypair (base58-encoded secret key)
# ⚠️  NEVER commit this to git. Add .env to .gitignore.
# Generated keypair public key: AjdHrwHUVTu57Br3AZpEstDFdqE6Knh2LG6EtfTaAwnJ
BACKEND_PRIVATE_KEY="<paste_secret_key_base58_from_backend-authority.json>"

# JWT Signing Secret — use a long random string in production
JWT_SECRET="aeterna-dev-secret-change-in-prod"

# Service Config
PORT=3001
NODE_ENV="development"
